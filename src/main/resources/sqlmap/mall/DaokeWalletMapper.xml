<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="daokeWallet">
    <!--根据道客钱包-->
   <select id="queryDaokeWallet" resultType="daokeWallet">
            SELECT * FROM daokeWallet WHERE  isValid=1
   </select>

</mapper>